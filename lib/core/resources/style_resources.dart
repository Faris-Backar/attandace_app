import 'package:flutter/material.dart';

class StyleResources {
  static Color primaryColor = Colors.grey[900]!;
  static const secondaryColor = Colors.white;
  static const teritiaryColor = Colors.black;
  static const errorColor = Colors.red;
  static const accentColor = Colors.white;
}
