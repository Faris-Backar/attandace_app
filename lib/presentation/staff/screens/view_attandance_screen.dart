import 'package:flutter/material.dart';

class ViewAttandanceScreen extends StatelessWidget {
  static const routeName = '/ViewAttandanceScreen';
  const ViewAttandanceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
