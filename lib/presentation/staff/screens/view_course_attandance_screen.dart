import 'package:flutter/material.dart';

class ViewCourseAttandanceScreen extends StatelessWidget {
  const ViewCourseAttandanceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Course'),
      ),
    );
  }
}
